'use client';

type FloatingCalculatorProps = {
  total?: number;
  breakdown?: any;
};

export function FloatingCalculator(props: FloatingCalculatorProps) {
  // Временная заглушка. Потом сюда перенесём настоящий калькулятор из Figma.
  return null;
}